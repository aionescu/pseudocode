{# using System.IO; #}

input <- "Input.txt"
output <- "Output.txt"

fileText <- {# File.ReadAllLines(input) #}

a <- {# int.Parse(filetext[0]) #}
b <- {# int.Parse(filetext[1]) #}

max <- {# Math.Max(a, b) #}

Scrie "Maximul este", max
{# File.WriteAllText(output, max.ToString()); #}

Scrie {# File.ReadAllText(output) #}