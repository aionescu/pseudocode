{# int lungime<T>(T[] a) { #}
  Returneaza {# a.Length #}
Sf

Subalgoritm f(x: intreg): intreg
  Returneaza x + 1
Sf

subalgoritm print(x: intreg)
  scrie x
sf

a <- Intreg[20]

print(lungime(a))

subalgoritm adauga(a: intreg, b: intreg): intreg
  returneaza a + b
sf

print(adauga(2, 7))